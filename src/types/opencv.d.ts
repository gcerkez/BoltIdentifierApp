declare namespace cv {
  class Mat {
    // Add relevant methods and properties as needed
    constructor();

    // Example methods
    delete(): void;

    clone(): Mat;

    // ...
  }

  // Declare other necessary classes and interfaces from OpenCV
  // For example:
  class Point {
    constructor(x: number, y: number);
  }

  // You can extend this as needed based on OpenCV usage
}
